fn main() {
    let mut first_name = String::from("Ivan ");
    let last_name = String::from("Mike");
    let full_name = first_name + &last_name;
    println!("{full_name}");

    // full_name.push()
}
